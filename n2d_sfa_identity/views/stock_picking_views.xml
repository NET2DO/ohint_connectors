<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="stock_picking_form_sfa" model="ir.ui.view">
        <field name="name">stock.picking.form.sfa.identity</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="salesperson_employee_id"/>
            </field>
        </field>
    </record>

    <record id="stock_picking_search_sfa" model="ir.ui.view">
        <field name="name">stock.picking.search.sfa.identity</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search"/>
        <field name="arch" type="xml">
            <!-- Odoo 19's stock.view_picking_internal_search has no group_by_user_id
                 filter; anchor after the Operation Type group-by instead. -->
            <filter name="picking_type" position="after">
                <filter name="sfa_field_salesperson" string="Field Salesperson"
                        context="{'group_by': 'salesperson_employee_id'}"/>
            </filter>
        </field>
    </record>
</odoo>
